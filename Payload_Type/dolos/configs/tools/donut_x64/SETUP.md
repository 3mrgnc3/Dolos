# Donut x64 Tool Setup

Donut is a shellcode generator that converts .NET assemblies into position-independent shellcode.

It does **not** need Python installed. Place `donut.exe` at `C:\tools\donut.exe` on the remote Windows server.

Download from: https://github.com/TheWover/donut/releases
