# Passthrough Tool Setup

The Passthrough encoder requires Python, same as PyEncoder.

Place `passthrough_encoder.py` at `C:\tools\passthrough_encoder.py` on the remote Windows server.
Python is installed automatically when `install_tools: true` is set in the profile.
