{
    "version": 2,
    "label": "PyEncoder",
    "enabled": true,
    "command": "py.exe C:\\tools\\dolos\\encoder.py {workdir}\\{input} {workdir}\\{output}",
    "ssh_host": "127.0.0.2",
    "ssh_port": 22,
    "ssh_username": "mrgnc",
    "ssh_password": "it800m3rz!windows",
    "ssh_key_enabled": false,
    "ssh_key_secret": "dolos_ssh_key",
    "timeout": 300,
    "success_string": "ENCODING_SUCCESS",
    "fail_string": "ENCODING_FAILED",
    "install_tools": true,
    "bypass_refs": [],
    "notes": "Windows-only encoder using py.exe. Password auth configured for dev. For production: set ssh_password to empty, enable ssh_key_enabled=true, and add your SSH private key as 'dolos_ssh_key' in Mythic UI → User Settings → Secrets."
}